#include "configuration/configuration.h"

mpireoderinglib::Configuration::Configuration (int* argc, char*** argv) : alg {STENCIL_STRIPS} {
	const char* env_var = getenv(env_flag);
	if (env_var) {
		
	}
}
